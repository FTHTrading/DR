[build]
  base    = "dashboard"
  command = "npm ci && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  CI           = "true"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[dev]
  framework = "next"
  port      = 3000
